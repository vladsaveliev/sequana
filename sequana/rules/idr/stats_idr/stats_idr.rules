rule stats_idr:
    """
    Some statistiques on Irreproducibility Discovery Rate
    Produce a json file for multiqc

    Required input:
        __stats_idr__input1 : all peaks files by condition

    Required output:
        __stats_idr__output: stat on idr output file


    """
    input:
         __stats_idr__input1
    output:
        __stats_idr__output
    log:
        __stats_idr__log
    shell:
        """
        some python code:
        for each file in list of {input}:
            if Rep in file:
                stats on file
            if SPR in file:
                stats on file
            if PPR in file:
                stats on file
        """

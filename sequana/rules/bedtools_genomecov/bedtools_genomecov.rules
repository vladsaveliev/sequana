rule bedtools_genomecov:
    """ Bedtools with genomecov option is used to generate a bed files with 
    coverage for each base position.

    Required input:
        __bedtools_genomecov__input: sorted bam file

    Required output:
        __bedtools_genomecov__output: bed file

    Contributors:
        Sequana consortium
    """
    input:
        __bedtools_genomecov__input
    output:
        __bedtools_genomecov__output
    shell:
        """
        bedtools genomecov -d -ibam {input} > {output}
        """
